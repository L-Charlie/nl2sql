# 本目录维护规则

- Git 检查必须限定当前项目路径，例如 `git status --short -- .`、`git diff --check -- .`；本目录属于上层仓库，无路径限定会扫描其他项目的历史改动。
- 测试依赖安装进程必须确认退出成功，并验证目标解释器可导入依赖后，再启动 pytest；安装器打印成功信息不代表目标目录复制已经完成。
- 修改状态定义或模型输入字段时，同步检查 `agent_team/state_prompts.py` 及实际调用的 system 消息；验证应覆盖模型消息内容，不能只检查模板文件。
